<div class="mx-auto max-w-6xl px-4 py-10">
  <h1 class="text-center text-lg font-semibold tracking-wide text-slate-700">投稿一覧</h1>

  <% if @posts.any? %>
    <div class="mt-8 grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
      <% @posts.each do |post| %>
        <article class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">
          <!-- 県 / カテゴリ -->
          <div class="flex items-center justify-between bg-[#92B099] px-4 py-2 text-sm text-white">
            <div class="flex items-center gap-2">
              <span class="font-medium"><%= post.prefecture_name %></span>
              <span class="text-white/60">の</span>
              <span class="font-medium"><%= post.category.name %></span>
            </div>
          </div>

          <div class="p-4">
            <!-- ユーザー -->
            <div class="flex items-center gap-3">
              <!-- アイコン（仮） -->
              <div class="h-10 w-10 rounded-full bg-slate-200"></div>

              <div class="min-w-0">
                <p class="truncate text-sm font-semibold text-slate-700">
                  <%= post.user.name %>
                </p>
                <p class="text-xs text-slate-500">
                  <%= post.place_name.presence || "場所未設定" %>
                </p>
              </div>
            </div>

            <!-- 本文 -->
            <p class="mt-3 text-sm leading-relaxed text-slate-700 line-clamp-3">
              <%= post.body %>
            </p>

            <!-- 写真（仮） -->
            <div class="mt-4 overflow-hidden rounded-xl border border-slate-200 bg-slate-50">
              <div class="flex aspect-[16/9] items-center justify-center text-xs text-slate-400">
                写真
              </div>
            </div>

            <!-- リアクション（仮） -->
            <div class="mt-4 flex items-center justify-between">
              <div class="flex gap-2">
                <button class="rounded-full border border-slate-200 px-3 py-1 text-sm text-slate-600 hover:bg-slate-50">
                  👀
                </button>
                <button class="rounded-full border border-slate-200 px-3 py-1 text-sm text-slate-600 hover:bg-slate-50">
                  ❤
                </button>
                <button class="rounded-full border border-slate-200 px-3 py-1 text-sm text-slate-600 hover:bg-slate-50">
                  📍
                </button>
              </div>

              <span class="text-xs text-slate-500">
                <%= l(post.created_at, format: :short) rescue post.created_at.strftime("%Y/%m/%d") %>
              </span>
            </div>
          </div>
        </article>
      <% end %>
    </div>
  <% else %>
    <p class="mt-12 text-center text-sm text-slate-500">
      まだ投稿がありません。
    </p>
  <% end %>
</div>
